services:
  - type: web
    name: borrower-metrics
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: "10000"
      - key: SECRET_KEY
        generateValue: true
    autoDeploy: true
